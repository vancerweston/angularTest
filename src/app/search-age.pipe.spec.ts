import { SearchAgePipe } from './search-age.pipe';

describe('SearchAgePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchAgePipe();
    expect(pipe).toBeTruthy();
  });
});
