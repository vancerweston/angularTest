import { SearchGenPipe } from './search-gen.pipe';

describe('SearchGenPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchGenPipe();
    expect(pipe).toBeTruthy();
  });
});
