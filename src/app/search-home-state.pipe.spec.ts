import { SearchHomeStatePipe } from './search-home-state.pipe';

describe('SearchHomeStatePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchHomeStatePipe();
    expect(pipe).toBeTruthy();
  });
});
